{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Restart api-gateway on port 3000",
			"type": "shell",
			"command": "powershell",
			"args": [
				"-Command",
				"$pid = (Get-NetTCPConnection -LocalPort 3000 -State Listen | Select-Object -First 1 -ExpandProperty OwningProcess); if ($pid) { Stop-Process -Id $pid -Force; Start-Sleep -Seconds 1 }; npm run start:prod"
			],
			"isBackground": true,
			"problemMatcher": [
				"$tsc"
			],
			"group": "build"
		}
	]
}